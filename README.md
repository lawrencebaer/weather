# weather
Larry's weather app

```bash
# clone the repo
$ git clone https://github.com/lawrencebaer/weather.git weather

# change directory
$ cd weather

# install the dependencies with npm
$ npm install

# start the server
$ npm start